# Plan Summary: bun-conversion

## What was planned and why

Convert `@sdlcforge/core-server` from npm to Bun for **package management**, following the pattern already proven in the sibling project `sdlcpilot-cli`, and adopt an XDG-compliant server configuration root along the way.

This plan covers core-server's own conversion only. It is item 3 of a larger modernization backlog; the full seven-phase modernization synthesis is background context, not scope.

### What must change

- `bun install` and `bun.lock` replace `npm install` and `package-lock.json`.
- Wildcard (`*`) dependency specifiers become explicit semver ranges. This is not cleanup — it is a hard prerequisite, established below.
- A repeatable procedure must exist for provisioning the gitignored `file:.yalc/…` dependencies into a task worktree, and the CI and Docker positions must be recorded explicitly rather than left implicit.
- `serverConfigRoot` moves from the package-relative `myPackagePath` to `${XDG_DATA_HOME}/sdlcforge-core/`, resolved through a new `@liquid-labs/comply-defaults` accessor.
- The Catalyst makefile toolchain must be *verified* to resolve and run correctly against a Bun-installed `node_modules`.
- All three test tiers must pass.
- `README.md`, `AGENTS.md`, `CLAUDE.md`, `docs/architecture.md`, `docs/core-server-spec.md`, and `docs/project-structure.md` must stop describing an npm-managed project with a package-relative config root.

### What must not change

- **The runtime contract.** `dist/sdlcforge-server-exec.js` keeps `#!/usr/bin/env -S node --enable-source-maps`, `engines.node` stays `>=18.0.0`, and the Docker Node 18–24 matrix stays the meaningful compatibility contract. Bun is a *development* dependency of this project, never an install-time requirement for consumers. The [runtime-target decision](./notes/runtime-target-decision.md) records this and defers a bun-if-available launcher to a followup outside this plan.
- **The dual build-artifact contract.** `dist/sdlcforge-server.js` (library, from `src/lib/index.js`) and `dist/sdlcforge-server-exec.js` (executable, from `src/cli/index.js`) both survive, with the same entry points and the same externals-only shape.
- **The Makefile/Catalyst build convention.** Rollup and Babel stay, and `make/*.mk` is not hand-edited at all — the [compatibility research](./notes/catalyst-bun-compatibility.md) found no change is required, and these files are generated by `@liquid-labs/catalyst-lib-makefiles` / `@liquid-labs/catalyst-builder-node`, so a hand-edit would be lost on regeneration.
- **The three-tier test structure**, and the unit tier's runner. Jest continues to run under Node against a Bun-installed `node_modules`. A `bun test` migration is technically viable but is explicitly **out of scope** — the rationale is recorded below.
- **The server's HTTP API surface.** The golden-api-spec characterization test (`src/lib/test/golden-api-spec.test.js` plus `test/__snapshots__/`) is the regression harness for this and must keep passing unmodified.
- **The plugin architecture.** Three tiers, 13 explicit plugins, runtime discovery and dynamic install, all unchanged.

### Success criteria

1. `bun install` succeeds from a clean checkout (given the documented `.yalc/` provisioning step) and produces a working `node_modules`.
2. `make build` produces both `dist/` artifacts with unchanged entry points, unchanged externals-only output, and the unchanged `node` shebang.
3. `make test`, `bun run test:local`, and `bun run test:integration` all pass, including the golden-api-spec snapshots byte-for-byte.
4. `make lint` and `make qa` pass.
5. `package-lock.json` is gone; `bun.lock` is committed.
6. No `*` version specifier remains in `package.json`.
7. The server resolves its configuration root to `${XDG_DATA_HOME}/sdlcforge-core/` (defaulting `XDG_DATA_HOME` to `${HOME}/.local/share`), guarded against an undefined `HOME`, through `@liquid-labs/comply-defaults`.
8. Documentation matches reality.

Four phases. Phases 1 through 3 are strictly sequential — each leaves the project in a working state and each depends on the previous phase's output. Phase 4 documents the result.

### Phase 1 — Bun package-management baseline

Makes `bun install` work, and makes the dependency tree reproducible somewhere other than the author's machine. Also lands the `serverConfigRoot` change, which is placed here because its first task is cross-repository and carries publish lead time that should start as early as possible.

Tasks:

1. **`001-pin-dependencies-and-adopt-bun-lockfile`** — re-derive an explicit range for each of the 12 wildcard specifiers against currently-published versions, generate `bun.lock`, delete `package-lock.json`.
2. **`002-establish-yalc-provisioning-procedure`** — a checked-in provisioning script plus the corrected post-`yalc push` instruction and the recorded CI policy. Depends on task 001.
3. **`003-add-server-config-root-accessor-to-comply-defaults`** — **executes in the `@liquid-labs/comply-defaults` repository, not this one.** Adds and publishes `COMPLY_SERVER_CONFIG_ROOT()`. Independent of tasks 001 and 002, so parallel-eligible with task 001.
4. **`004-adopt-xdg-server-config-root`** — consumes the new accessor in `app-init.mjs` and preserves the packaged `server-settings.yaml` defaults. Depends on tasks 001 and 003.

The build and test toolchain is deliberately left alone here, which keeps the phase's blast radius on dependency resolution and yields a clean before/after signal.

**Exit state:** `bun install` succeeds; `make build`, `make test`, and `make lint` still pass; the server resolves its config root through comply-defaults.

### Phase 2 — Build and test toolchain verification

Confirms, in the real repository rather than the spike's scratch copy, that the Catalyst toolchain works against the Bun-installed tree — and records the "no changes required" determination so it is not re-litigated. The spike used illustrative pins, a scratch copy, and a pre-populated `dist/`; this phase retires the residual risk that the real repository behaves differently.

Tasks, both parallel-eligible:

1. **`001-verify-build-and-lint-under-bun`** — `make build`, `make lint`, `make qa`, and artifact-shape verification.
2. **`002-verify-unit-test-tier`** — `make test`, golden snapshots byte-for-byte, coverage into `qa/coverage/`, and the written record of the Jest-stays decision.

**Exit state:** `make build`, `make test`, `make lint`, and `make qa` all pass against a `bun install`-produced `node_modules`, with evidence captured and no `make/*.mk` file modified.

### Phase 3 — Runtime confirmation and integration tiers

Brings the two integration tiers and the operational scripts into line. The runtime decision is a no-change, so this phase *confirms* the shebang and `engines` rather than altering them, and converts the npm invocations that surround them.

Tasks, both parallel-eligible:

1. **`001-convert-local-integration-tier`** — `scripts/test.sh` and `scripts/start.sh`; `bun run test:local` passing against the new config root.
2. **`002-convert-docker-integration-tier`** — `test/run-integration-tests.sh`, the bind-mount architecture confirmation, `test/test-ci.sh`, and deletion of the dead `test/setup-local-deps.sh`.

**Exit state:** all three test tiers pass; `bun run start` / `bun run stop` work; the Node 18–24 matrix is intact.

### Phase 4 — Documentation updates

The conversion changes spec-defined behavior (the configuration root, and the package-management story), the tech stack and build-pipeline descriptions in `docs/architecture.md`, and every build/test/run command in `AGENTS.md`, `CLAUDE.md`, and `README.md`. `docs/project-structure.md` still lists `package-lock.json`.

Tasks, both parallel-eligible:

1. **`001-update-architecture-docs`** — `docs/architecture.md` and `docs/core-server-spec.md`.
2. **`002-update-contributor-and-consumer-docs`** — `AGENTS.md`, `CLAUDE.md`, `README.md`, `docs/project-structure.md`.

### Parallelism

No parallelism across phases — Phase 2 needs Phase 1's resolvable dependency tree, Phase 3 needs Phase 2's verified build, and Phase 4 documents what the first three produced. Within phases:

| Phase | Parallel-eligible groups | Sequencing |
|---|---|---|
| 1 | `001` ∥ `003` | `002` after `001`; `004` after `001` and `003` |
| 2 | `001` ∥ `002` | — |
| 3 | `001` ∥ `002` | — |
| 4 | `001` ∥ `002` | — |

## What shipped

### Phase 01 — Bun Package Management Baseline

1. **Pin Dependencies And Adopt Bun Lockfile** (`001-pin-dependencies-and-adopt-bun-lockfile.md`, tier `sonnet-high`) — Re-derived explicit ^1.0.0-alpha.N pins for all 12 previously-wildcarded dependency specifiers in package.json. Ran bun install to generate bun.lock and git rm'd package-lock.json; clean-state reinstall confirmed deterministic resolution. Verified equivalence with make build, make test, make lint. Diff scoped exactly to package.json, bun.lock, and the package-lock.json deletion.
   Commit `27404f7`, merged at `58d99f2654685e9dc3d0c94b86849e8ae1f30cde`.

2. **Establish Yalc Provisioning Procedure** (`002-establish-yalc-provisioning-procedure.md`, tier `sonnet-high`) — Added scripts/provision-local-deps.sh, an idempotent set -e Bash script that resolves the main checkout via git rev-parse --git-common-dir, copies .yalc/+yalc.lock in from there when absent locally, fails with an actionable message when .yalc/ is absent everywhere, supports --refresh-lock, and finishes by running bun install with exit status propagated. Corrected the post-yalc-push instruction in AGENTS.md at both locations, explaining why a bare bun install is insufficient, and recorded the CI policy and worktree provisioning procedure. Validated end-to-end via a throwaway git worktree exercising failure path, happy path, idempotent re-run, and --refresh-lock, all passing.
   Commit `da8acb7`, merged at `aed9fc11d937cb2d81a834723350ee1ea11a9c92`.

3. **Add Server Config Root Accessor To Comply Defaults** (`003-add-server-config-root-accessor-to-comply-defaults.md`, tier `sonnet-high`) — Added COMPLY_SERVER_CONFIG_ROOT() to comply-defaults' src/locations.mjs, resolving ${XDG_DATA_HOME:-<home>/.local/share}/sdlcforge-core per the XDG Base Directory spec, with a guarded home-directory resolver (os.homedir() fallback, then a named ComplyConfigResolutionError as last resort). Extended the same guard to the pre-existing unguarded configDir used by COMPLY_HOME(); verified no behavior change for any environment where HOME is set. Merged to comply-defaults main at 29fd3d5. Published as 1.0.0-alpha.9, confirmed via npm view @liquid-labs/comply-defaults versions --json. Task 004 can now consume this via core-server's existing ^1.0.0-alpha.8 range with no package.json edit needed.
   Commit `283c72c`.

4. **Adopt XDG Server Config Root** (`004-adopt-xdg-server-config-root.md`, tier `sonnet-high`) — Implemented the XDG-based serverConfigRoot switch in src/lib/app-init.mjs, consuming COMPLY_SERVER_CONFIG_ROOT() from @liquid-labs/comply-defaults@1.0.0-alpha.9, and adding a first-run-only, non-fatal seedServerSettings helper that copies the packaged server-settings.yaml into the new config root. Seeding is skipped when a caller supplies its own serverConfigRoot, keeping both existing unit tests unaffected. bun.lock refreshed via bun update; package.json specifier left at ^1.0.0-alpha.8. All in-scope validation passed; diff confined to src/lib/app-init.mjs and bun.lock. This completes Phase 1 of the bun-conversion plan.
   Commit `ea35dbd`, merged at `7afdf770e22e8a066167aa6cdfc94af27b1ff723`.

### Phase 02 — Bun Build And Test Toolchain

1. **Verify Build And Lint Under Bun** (`001-verify-build-and-lint-under-bun.md`, tier `sonnet-med`) — Re-ran the full verification from a genuinely clean state (895 packages installed, exit 0). All three npm explore config-path lookups resolve correctly, node_modules shape matches prediction. make build produces both dist bundles at exactly 12 lines each (the corrected figure, reflecting phase 1 task 004's legitimate XDG serverConfigRoot/seedServerSettings addition) - still externals-only in substance (grep shelljs = 0). make lint and make qa both surface the same pre-existing lint debt rather than any Bun-specific regression; unit-test tier passed cleanly. No make/*.mk file touched.
   Commit `8aca822`, merged at `7ae922b2b33a7975638859748da2e064907fcc0c`.

2. **Verify Unit Test Tier** (`002-verify-unit-test-tier.md`, tier `sonnet-med`) — Ran make test in the Bun-provisioned worktree (895 packages via bun install) and confirmed the full pipeline works unchanged: Babel test-staging transpile, then Jest under Node, producing 3 passed suites / 5 passed tests. Golden snapshots byte-identical. qa/coverage/ carries the full Istanbul directory shape that sdlc-projects-badges-coverage depends on downstream. No file under make/, src/, or test/ touched - only the task document's Status section, recording observed counts/coverage (66.66% stmts / 57.89% branch / 50% funcs / 70% lines) and the four runner-decision statements. Confirmed but did not touch the latent process.env.COMPLY_HOME mutation in src/lib/test/app-init.test.js per requirement 5 (surface, don't fix) - this duplicates the existing followup zNBA, no new followup needed.
   Commit `b4b9573`, merged at `c00291a7b2698a0ae6b2121d411ccbb5a65f17c8`.

### Phase 03 — Bun Runtime And Integration Tests

1. **Convert Local Integration Tier** (`001-convert-local-integration-tier.md`, tier `sonnet-med`) — Converted scripts/test.sh (npm run build/stop -> bun run build/stop) and scripts/start.sh's stop-instruction text; confirmed scripts/stop.sh needs no change; reconfirmed the runtime-target no-change decision. Verified the real server starts and writes server-settings.yaml with the packaged registries: entry under both scratch XDG_DATA_HOME and unset-XDG_DATA_HOME/scratch-HOME fallback, stops cleanly in both - first time a running server exercises phase 1 task 004's XDG config-root change. bun run test:local passed 7/7. make test still passes.
   Commit `19893de`, merged at `0f4729658ea3da7b05cde1fd964ad6fce4c109c3`.

2. **Convert Docker Integration Tier** (`002-convert-docker-integration-tier.md`, tier `sonnet-high`) — Converted test/run-integration-tests.sh's host-side build to bun run build; deleted dead test/setup-local-deps.sh; confirmed container needs no changes. bun run test:integration ran the full nine-version Node matrix, every version reporting failed:0 across all 7 endpoint checks, confirming the pathResolvers fix resolved the crash that blocked this task's prior attempt. bun.lock refreshed. make test still passes.
   Commit `25364ef`, merged at `f78660a6b5c6e08ce0e41cc0b951e7c30e9deb17`.

### Phase 04 — Documentation Updates

1. **Update Architecture Docs** (`001-update-architecture-docs.md`, tier `sonnet-high`) — Updated docs/architecture.md, docs/core-server-spec.md, and docs/architecture/plugin-loading-tiers.md to reflect the bun-conversion plan: Tech stack gained a Package management bullet; Configuration bullet names the new accessor; Core initialization rewritten for serverConfigRoot/XDG resolution and server-settings.yaml seeding; Build pipeline records makefiles were deliberately left untouched; Test/Test infrastructure corrected Alpine->Ubuntu; Key decisions' yalc bullet gained the bun.lock non-refresh trap. core-server-spec.md's build/test commands converted to Bun forms. plugin-loading-tiers.md's stale code sample corrected. All three stale-doc followups (serverHome naming, Alpine description, Tech Stack gap) addressed.
   Commit `e53e939`, merged at `9333496f49c96db7e165a3212f05987712e2e891`.

2. **Update Contributor And Consumer Docs** (`002-update-contributor-and-consumer-docs.md`, tier `sonnet-med`) — Converted every build/test/lint/run command in AGENTS.md and CLAUDE.md to Bun form, added toolchain-prerequisite explanation and getting-started line, added COMPLY_SERVER_CONFIG_ROOT() to both docs, corrected Troubleshooting to rm -f bun.lock && bun install remedy. docs/project-structure.md shows bun.lock instead of package-lock.json, lists provision-local-deps.sh. test/README.md's stale Alpine description corrected to Ubuntu. AGENTS.md yalc section left byte-for-byte untouched.
   Commit `62e7480`, merged at `2b3cb7cef72ceb30031596b1ed5e1a0c26a9de2a`.

## Key decisions

_No `## Why this shape` section is recorded in `plan/overview.md`, so this plan's cross-task rationale was never written down. Per-task outcomes are under "What shipped" above._

## Follow-up items

- **`AFcz`** — **bun-if-available launcher** — dist/sdlcforge-server-exec.js keeps a plain `node` shebang for this plan (per user decision during bun-conversion planning: no consumer-facing runtime requirement change). Follow-up: introduce a launcher script that detects and prefers `bun` at runtime when available, falling back to `node` otherwise. Deferred out of the bun-conversion plan's scope.

- **`zNBA`** — **Test env-var isolation fragility** — src/lib/test/app-init.test.js mutates process.env.COMPLY_HOME process-wide in beforeAll. Currently masked by Jest's per-file worker isolation; surfaced during bun-conversion research when two copies of the test tree were discovered together and raced on the shared env var. Pre-existing, unrelated to Bun — worth hardening test isolation regardless of runtime.

- **`b3hk`** — **make lint-fix surfaces 231 pre-existing lint** — make lint-fix surfaces 231 pre-existing lint violations confined to test/*.js (unrelated to this task, matching the plan's own WIP-branch-triage finding that they're stale/regenerable). Not touched here since test/ is out of this task's scope; noted in case a future phase wants to clean them up deliberately.

- **`0qOH`** — **provision-local-deps.sh misses 2 .yalc deps** — Phase 1 security review (phase-review, bun-conversion): scripts/provision-local-deps.sh's presence check and failure message only account for @liquid-labs/plugable-express and @liquid-labs/liq-projects, but bun.lock actually resolves two more file:.yalc/... deps transitively: @liquid-labs/github-toolkit (via liq-work) and @liquid-labs/http-smart-response (via plugable-projects-audit). A checkout with only the two documented packages under .yalc/ will still fail bun install deep in resolution, with no actionable guidance. AGENTS.md's new CI policy statement ("CI does not attempt to resolve file:.yalc/... links") is also not achievable as stated for liq-work/plugable-projects-audit at their pinned versions. Root fix may be requesting upstream republish of those two packages with proper registry-resolvable deps, or extending the script's coverage — needs a decision, not a mechanical fix.

- **`YZIu`** — **Symlink hardening in seedServerSettings** — Phase 1 security review (phase-review, bun-conversion), low confidence/suggestion: src/lib/app-init.mjs's seedServerSettings() checks existsSync(destPath) then copyFile with no exclusive-create flag; a dangling symlink at the destination would have its target overwritten. Requires attacker to already have write access to the parent dir (normally single-user $HOME/.local/share) — not new remote attack surface, mirrors plugable-express's own existing write-if-absent pattern. If hardening is desired: use fs.constants.COPYFILE_EXCL and treat failure as a normal non-fatal seed failure.

- **`PAof`** — **Symlink hardening in provision-local-deps.sh** — Phase 1 security review (phase-review, bun-conversion), low confidence/suggestion: scripts/provision-local-deps.sh's `cp -R "$MAIN_CHECKOUT/.yalc" "$CURRENT_DIR/.yalc"` preserves symlinks rather than dereferencing. If the main checkout's gitignored .yalc/ ever contained a symlink pointing outside the intended tree, this script propagates it into every new task worktree. Requires attacker to already have write access to the developer's local .yalc/ — pre-existing local-compromise-level trust, not new remote attack surface. If hardening is desired: use `cp -RL` or add a post-copy symlink rejection check.

- **`XpNW`** — **Primary blocker: liq-credentials@^1.0.0-alpha** — Primary blocker: liq-credentials@^1.0.0-alpha.3 is incompatible with the yalc'd plugable-express@1.0.0-alpha.57 - ext.pathResolvers never initialized. Blocks any real server start until pin revisited or plugable-express fixed.

- **`vDJG`** — **Secondary: a stale global bun link at ~/.bun/** — Secondary: a stale global bun link at ~/.bun/bin/sdlcforge-server points at an old build; test/test-server.js's bare-name spawn picks it up via PATH.

- **`HHGR`** — **Pre-existing, blocking defect (not introduced** — Pre-existing, blocking defect (not introduced by this task): explicit-plugin loading crashes at server startup because yalc-linked plugable-express never initializes app.ext.pathResolvers, which liq-credentials (and likely liq-integrations/liq-orgs/liq-work/liq-projects) requires. Blocks both this task's Docker integration tier and phase-03 task-001's local integration tier, since both start the real server with the full explicit-plugin set. Same defect category as followups ynGa/26sW (about app.ext.serverHome/serverConfigRoot) but manifests as app.ext.pathResolvers - may be related-but-distinct, worth re-verifying against current plugable-express. Needs a fix inside plugable-express (yalc-linked external package) and/or affected liq-* plugins before Docker/local integration tiers can pass.

- **`KzUv`** — **Requirement 4/Validation's literal repo-wide-** — Requirement 4/Validation's literal repo-wide-grep-zero-hits framing for setup-local-deps does not account for this task's worktree carrying the full plan/ tree (branched from plan worktree) - the plan's own description of the deletion will always self-match. Worth tightening the check's wording in future task-doc authoring.

- **`ZE9R`** — **Stale global ~/.bun/bin/sdlcforge-server syml** — Stale global ~/.bun/bin/sdlcforge-server symlink (dated Aug 6, points at main checkout not any worktree) will shadow test/test-server.js's bare-name spawn() for future bun run test:local invocations that don't override PATH. Outside this worktree's scope; worth refreshing/removing the global link.

- **`uXoI`** — **Observed: launching bun run test:local twice** — Observed: launching bun run test:local twice back-to-back can race on port 32600 (EADDRINUSE) because the prior run's server process isn't always fully torn down. Unrelated to this task's script changes; noting in case it surfaces in CI-style rapid-fire runs.

- **`KWnu`** — **Pre-existing bootstrap gap unrelated to this** — Pre-existing bootstrap gap unrelated to this task: a genuinely fresh developer machine (or fresh $HOME) hitting bun run start for the first time crashes with ENOENT writing api.json via COMPLY_HOME()/COMPLY_API_SPEC_PATH() in @liquid-labs/comply-defaults, because the parent directory isn't created first. Needs a fix in comply-defaults, out of this task's scope.

- **`mm89`** — **CLAUDE.md is not linked from README.md's Addi** — CLAUDE.md is not linked from README.md's Additional documentation list - pre-existing, predates this task. Uncertain whether this is a deliberate convention (CLAUDE.md as agent-tooling metadata, common to exclude from user-facing doc index) or a genuine link-chain gap - needs a judgment call, not a mechanical fix.

- **`w67o`** — **plugin-loading-tiers.md sample shape stale** — Phase 4 correctness review (phase-review, bun-conversion), informational/pre-existing: docs/architecture/plugin-loading-tiers.md's Configuration surface code sample still shows appInit as a single-expression arrow function, while the actual src/lib/app-init.mjs has had a block body wrapping a conditional seedServerSettings() call since before this plan's diff_base. This task correctly renamed serverHome->serverConfigRoot in the sample and added prose pointing to the accurate description elsewhere, but didn't update the sample's function-shape. Pre-existing drift, not introduced or worsened by this plan; low-priority doc-maintenance item.

## Final Task State

# TODO

## Purpose and scope

Tracking document for the active plan.

## Tasks

### Phase 01 — Bun Package Management Baseline

- [x] [001-pin-dependencies-and-adopt-bun-lockfile.md](./phase-01-bun-package-management/001-pin-dependencies-and-adopt-bun-lockfile.md) — tier `sonnet-high` · branch `plan/bun-conversion-01-001` · commit `27404f7` · merge `58d99f2654685e9dc3d0c94b86849e8ae1f30cde`
- [x] [002-establish-yalc-provisioning-procedure.md](./phase-01-bun-package-management/002-establish-yalc-provisioning-procedure.md) — tier `sonnet-high` · branch `plan/bun-conversion-01-002` · commit `da8acb7` · merge `aed9fc11d937cb2d81a834723350ee1ea11a9c92`
- [x] [003-add-server-config-root-accessor-to-comply-defaults.md](./phase-01-bun-package-management/003-add-server-config-root-accessor-to-comply-defaults.md) — tier `sonnet-high` · branch `task/add-server-config-root-accesso` · commit `283c72c` · merge `…`
- [x] [004-adopt-xdg-server-config-root.md](./phase-01-bun-package-management/004-adopt-xdg-server-config-root.md) — tier `sonnet-high` · branch `plan/bun-conversion-01-004` · commit `ea35dbd` · merge `7afdf770e22e8a066167aa6cdfc94af27b1ff723`

### Phase 02 — Bun Build And Test Toolchain

- [x] [001-verify-build-and-lint-under-bun.md](./phase-02-bun-build-and-test-toolchain/001-verify-build-and-lint-under-bun.md) — tier `sonnet-med` · branch `plan/bun-conversion-02-001-2` · commit `8aca822` · merge `7ae922b2b33a7975638859748da2e064907fcc0c`
- [x] [002-verify-unit-test-tier.md](./phase-02-bun-build-and-test-toolchain/002-verify-unit-test-tier.md) — tier `sonnet-med` · branch `plan/bun-conversion-02-002` · commit `b4b9573` · merge `c00291a7b2698a0ae6b2121d411ccbb5a65f17c8`

### Phase 03 — Bun Runtime And Integration Tests

- [x] [001-convert-local-integration-tier.md](./phase-03-bun-runtime-and-integration/001-convert-local-integration-tier.md) — tier `sonnet-med` · branch `plan/bun-conversion-03-001-2` · commit `19893de` · merge `0f4729658ea3da7b05cde1fd964ad6fce4c109c3`
- [x] [002-convert-docker-integration-tier.md](./phase-03-bun-runtime-and-integration/002-convert-docker-integration-tier.md) — tier `sonnet-high` · branch `plan/bun-conversion-03-002-2` · commit `25364ef` · merge `f78660a6b5c6e08ce0e41cc0b951e7c30e9deb17`

### Phase 04 — Documentation Updates

- [x] [001-update-architecture-docs.md](./phase-04-doc-updates/001-update-architecture-docs.md) — tier `sonnet-high` · branch `plan/bun-conversion-04-001` · commit `e53e939` · merge `9333496f49c96db7e165a3212f05987712e2e891`
- [x] [002-update-contributor-and-consumer-docs.md](./phase-04-doc-updates/002-update-contributor-and-consumer-docs.md) — tier `sonnet-med` · branch `plan/bun-conversion-04-002` · commit `62e7480` · merge `2b3cb7cef72ceb30031596b1ed5e1a0c26a9de2a`
