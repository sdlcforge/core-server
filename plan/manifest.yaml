version: 1
plans:
    - slug: pluggable-defaults-rename
      status: working
      dependencies:
        /Users/zane/playground/liquid-labs/pluggable-defaults:
            phase: TBD
            task: TBD
            note: liq-work depends on @liquid-labs/plugable-defaults@^1.0.0-alpha.4, which resolves fine today (published Nov 2023, no live bug). Depends on pluggable-defaults's own naming fix and any decision to publish newer (currently unpublished) content under the corrected single-g name, so this project's lockfile can pick up the latest code once available.
      wave:
        slug: sdlcforge-modernization
        leadProject: core-server
